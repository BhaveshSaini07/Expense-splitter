this is expense spilliter.
Expense Splitter — HTML, CSS, JavaScript
Built a bill-splitting web app for roommates/groups; calculates 
per-person balances using array methods (filter, reduce). 
Features dynamic DOM rendering, input validation, and CSS 
custom properties for theming.
